import {StyleSheet} from 'react-native';

const createStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  button: {
    marginHorizontal: 50,
    alignItems: 'center',
  },
  buttonText: {
    color: 'white',
    fontSize: 20,
  },
  button: {
    marginHorizontal: 12,
    alignItems: 'center',
    padding: 8,
    borderRadius: 5,
  },
});

export default createStyles;
